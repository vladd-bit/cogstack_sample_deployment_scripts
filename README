Don't forget to execute chmod -R u+x ./path_to_this_folders_scripts*.sh otherwise you won't be executing much...

find $(pwd)/cogstack_pipeline -name "*.sh" -execdir chmod u+x {} +
find $(pwd)/utils -name "*.sh" -execdir chmod u+x {} +


Quickstart steps:
wget "repo https://github.com/....../master.zip"

unzip master.zip -O cogstack_deployment
mv ./cogstack_deployment /etc/cogstack_deployment

chmod -R 755 /etc/cogstack_deployment
chown -R root:root /etc/cogstack_deployment




The ``` db_samples.sql.gz ``` belongs to example9.