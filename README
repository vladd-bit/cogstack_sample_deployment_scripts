Don't forget to execute chmod -R u+x ./path_to_this_folders_scripts*.sh otherwise you won't be executing much...

find $(pwd)/cogstack_pipeline -name "*.sh" -execdir chmod u+x {} +
find $(pwd)/utils -name "*.sh" -execdir chmod u+x {} +